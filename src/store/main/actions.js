import { createAction } from '@store/actionCreators';

export const UPDATE_PROJECT_TYPE = createAction('main/UPDATE_PROJECT_TYPE_ACTION');
export const UPDATE_PROJECT_FILTER = createAction('main/UPDATE_PROJECT_FILTER_ACTION');
export const UPDATE_CURRENT_PAGE = createAction('main/UPDATE_CURRENT_PAGE');
